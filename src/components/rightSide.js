import React from 'react'
import Link from 'gatsby-link'

const RightSide = ({ siteTitle, currentOccupation }) => (
  <div
    style={{
      margin: 'auto',
      background: 'white',
      marginBottom: '1.45rem',
    }}
  >
    <div
      style={{
        margin: 'auto',
        maxWidth: 500,
        padding: '1.45rem 1.0875rem',
      }}
    >

  <h4>RightSide</h4>

    </div>
  </div>
)

export default RightSide
